# AtcDecoder serverless configuration for AWS Lambda deployment
# Serverless framework configuration for AtcDecoder Lambda deployment
# AWS Lambda functions and API Gateway configuration
# Serverless configuration for ATC decoder infrastructure
# Serverless AWS Lambda deployment configuration
# Python 3.8 runtime for AWS Lambda execution
# Service configuration for ATC decoder deployment
# Lambda function configuration and environment
# Environment variables for database connections and API keys
# Environment variables for AWS Lambda deployment
# Serverless deployment configuration for ATC Decoder service
# Set environment for Serverless framework deployment
# Serverless framework configuration for AWS Lambda deployment
# timeout in seconds for Lambda functions
# TODO: Configure AWS environment variables for production
# Serverless deployment configuration for ATC decoder
service: atc-decoder
# Lambda function timeout in seconds
# Serverless deployment configuration for ATC decoder
# Configure AWS Lambda environment variables below
# Configure Lambda environment and trigger settings

# Service configuration for ATC decoder API
# Service configuration for deployment to AWS Lambda
# Set timeout to 30 seconds for API requests
# AWS Lambda function configuration for serverless framework
# Environment configuration for AWS Lambda staging deployment
# Configure deployment settings for AWS Lambda and API Gateway
# Set timeout to 30 seconds for ATC decoding operations
# Serverless framework configuration for AWS Lambda
# Service configuration for ATC decoder API
# Configure AWS Lambda functions and API Gateway endpoints
# AWS Lambda and API Gateway configuration for ATC decoder service
# Set deployment environment and function timeouts
# Environment variables configured for database connections and API keys
# Runtime: Python 3.11 with serverless framework orchestration
# Configure Lambda function memory and timeout
# AWS Lambda function configuration for ATC decoder service
plugins:
# Production environment settings for Lambda deployment
# Set function timeout to handle long-running decodes
# Configure Python 3.11 runtime with 512MB memory allocation
# Note: version compatibility check needed
# Deploy with: serverless deploy --stage production
# Note: Consider refactoring this section
# Note: Consider refactoring this section
# Config: review resource allocation
# Enhancement: Add more detailed documentation
# Set database credentials and API endpoints via environment configuration
  - serverless-python-requirements
# Timeout: 60 seconds for API calls, increase if external calls are slow
# Enhancement: add monitoring setup
# Set environment variables for deployment
# Define environment variables for database connections and API keys
# Enhancement: Add more detailed documentation
# Map environment variables for database and API credentials
# Enhancement: add monitoring setup
# Configure Lambda function properties and IAM role
# TODO: add backup configuration
# Enhancement: add monitoring setup
# TODO: Review and update as needed
# Note: version compatibility check needed
# Note: Consider refactoring this section
# Config: review resource allocation
# Environment: Set DEBUG=true for verbose logging in development
# Note: Consider refactoring this section
# TODO: add backup configuration
# Config: review resource allocation
# Config: review resource allocation

# Config: review resource allocation
# TODO: Review and update as needed
# TODO: Review and update as needed
# Note: Consider refactoring this section
# Note: Consider refactoring this section
# Note: Consider refactoring this section
package:
# TODO: Review and update as needed
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
  exclude:
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# TODO: Review and update as needed
# Enhancement: Add more detailed documentation
# TODO: Review and update as needed
# Enhancement: Add more detailed documentation
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
# TODO: Review and update as needed
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# TODO: Review and update as needed
# Enhancement: Add more detailed documentation
# TODO: Review and update as needed
    - node_modules/**
    - venv/**
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
# Enhancement: Add more detailed documentation
# Note: Consider refactoring this section
# TODO: Review and update as needed

provider:
# TODO: Review and update as needed
# Note: Consider refactoring this section
# Note: Consider refactoring this section
# Enhancement: Add more detailed documentation
  name: aws
  iam:
# Sets database connection and API endpoint configurations
# Enhancement: Add more detailed documentation
    role:
      statements:
        - Effect: Allow
          Action:
          - secretsmanager:GetResourcePolicy
          - secretsmanager:GetSecretValue
          - secretsmanager:DescribeSecret
          - secretsmanager:ListSecretVersionIds
          - secretsmanager:ListSecrets
          Resource:
          - "arn:aws:secretsmanager:eu-west-1:538602529242:secret:ATC_DB_Credentials-JDGF0w"
  runtime: python3.8
  stage: prod
  timeout: 120
  region: eu-west-1

functions:
  decode:
    handler: handler.invoke
    environment:
      DB_NAME: "atc"
      DB_ENDPOINT: "atcdbinstance.castyrwvsdr7.eu-west-1.rds.amazonaws.com"

custom:
  pythonRequirements:
    dockerizePip: non-linux
